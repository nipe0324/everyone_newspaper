<span class="vote">
	評価:<%= article.votes %>
	<% if current_user && current_user.can_vote_for?(article) %>
	| <%= link_to "like", "/newspaper/#{article.id}/vote?value=1", method: :post %>
	| <%= link_to "unlike",  "/newspaper/#{article.id}/vote?value=1", method: :post %>
	<% end %>
</span>